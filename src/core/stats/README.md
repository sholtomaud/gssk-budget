# `src/core/stats`

Distributions, statistical tests and multiple-comparison adjustment. Goal variance never routes through here — a missed target falsifies nothing (REQ-EVA-1a). Built by `p4-stats-tests`.

Empty until the task that fills it. Listed in §9.1 of
`docs/gssk-budget-requirements.md`, so the directory exists from the start
rather than appearing halfway through and moving code around.
